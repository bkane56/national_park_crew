# National park collage photos

Drop 4–8 landscape photos here to populate the hero collage at the top of the planner UI.

Supported formats: `.jpg`, `.jpeg`, `.png`, `.webp`

Files are loaded automatically on app startup (sorted by filename). No code changes needed after adding images—restart the UI with `uv run run_ui`.

Use photos you own or have permission to display.
